# Workspace Agent Instructions

- Read this `AGENTS.md` file before starting work in this workspace.
- The user-provided Python 3.14 path is:
  `C:\Users\22203\AppData\Roaming\Microsoft\Windows\Start Menu\Programs\Python\Python 3.14`
- When Python is needed, check this location first. Because it may be a Start Menu
  shortcuts directory rather than the interpreter executable, inspect its contents
  and resolve the actual `python.exe` target before running Python.
